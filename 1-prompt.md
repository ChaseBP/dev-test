Generate a very simple, exam-ready Spring Boot project using:

- Spring Boot
- Thymeleaf
- Spring Data JPA
- H2 Database (in-memory)

Project Name: <PUT PROJECT NAME HERE>

Requirements:

- Must be VERY SIMPLE (for exams)
- Must be plug-and-play (no configuration issues)
- Only basic CRUD (Add + View is enough)
- Use MVC architecture (Controller, Entity, Repository)
- No unnecessary complexity
- Use clear and minimal code

Output format MUST be EXACTLY like this:

1. Title:

# ✅ <Project Name> (Spring Boot – Exam Ready)

2. Goal and Stack:
   👉 Goal: simple explanation  
   👉 Stack: Spring Boot + Thymeleaf + H2

3. Project Structure (tree format)

4. Code sections in order:

- Main Application
- Entity Class
- Repository
- Controller
- HTML Form (form.html)
- HTML List (list.html)
- application.properties
- Maven (pom.xml)
- Gradle (build.gradle)

5. Each section must:

- Have clear heading with emoji
- Include complete working code
- Use proper package naming based on project

6. HTML must:

- Use Thymeleaf (`xmlns:th`)
- Include form + table

7. Database:

- Use H2 only
- Include `ddl-auto=update`

8. Add final sections:

- ▶️ How to Run (Maven + Gradle)
- 🌐 URL to open
- ✅ Exam Points (short bullet points)

9. Code must:

- Compile without errors
- Run directly without modification
- Be minimal and clean

10. DO NOT:

- Add explanations between code
- Add advanced features (security, REST APIs, etc.)

11. Keep formatting EXACTLY like:

# ✅ Title

---

# 📁 Project Structure

---

# 🧠 1. Main Application

## (code)

...

End with:
👉 Say "go" for next project
